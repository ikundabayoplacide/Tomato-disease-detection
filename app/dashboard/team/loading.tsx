import { SplineIcon } from "lucide-react";

export default function Loading() {
    return(
        <div className="flex justify-center items-center h-screen">
            <SplineIcon size={30} color="black"/>
        </div>
    )
  }
  
  